<!DOCTYPE html>
<html lang="ru">
<head>
  {{> head}}
  <title>Вход — Эрмитаж</title>
</head>
<body>
  {{> header}}
  <main class="main">
    <div class="auth-page">
      <h1>Вход</h1>

      {{#if error}}
        <p class="auth-page__error">Неверное имя или пароль. Проверьте данные или <a href="/auth/register">зарегистрируйтесь</a>.</p>
      {{/if}}

      <form class="tickets__form" method="POST" action="/auth/login">
        <label class="tickets__label">
          Имя:
          <input type="text" name="name" required autocomplete="username">
        </label>
        <label class="tickets__label">
          Пароль:
          <input type="password" name="password" required autocomplete="current-password">
        </label>
        <button type="submit" class="tickets__btn">Войти</button>
      </form>

      <p class="auth-page__link">Нет аккаунта? <a href="/auth/register">Зарегистрироваться</a></p>
    </div>
  </main>
  {{> footer}}
</body>
</html>
